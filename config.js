window.STONE_TRACKER_CONFIG = {
  // Set these to enable multi-device sync with Supabase.
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",

  // Optional: use your hosted app URL so QR codes are scannable from any device.
  // Example: "https://stones.yourcompany.com"
  PUBLIC_APP_URL: "",
};
